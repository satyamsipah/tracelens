# Copy to .env.local for local development.
# Server components read TRACELENS_API_BASE; client components fall back to
# NEXT_PUBLIC_API_BASE since only NEXT_PUBLIC_* vars are exposed to the browser.
TRACELENS_API_BASE=http://localhost:8080
NEXT_PUBLIC_API_BASE=http://localhost:8080
