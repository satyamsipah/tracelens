apiVersion: 1

providers:
  - name: TraceLens
    orgId: 1
    folder: TraceLens
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/dashboards
